<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record model="ir.ui.view" id="view_hospital_appointment_form">
        <field name="name">hospital.appointment.form</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <field name="state" widget="statusbar"
                           options="{'clickable':1}"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1 class="d-flex">
                            <field name="priority" widget="priority" class="me-3"/>
                        </h1>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="bg-danger"
                            attrs="{'invisible': [('active', '=', True)]}"/>
                    <div class="oe_titel">
                        <h1>REF:
                            <field name="ref" readonly="1"/>
                        </h1>
                        <h2>
                            ID:
                            <field name="id" readonly="1"/>
                        </h2>
                    </div>
                    <group>
                        <group string="Appointment General">
                            <field name="ref"/>
                            <field name="sequence"/>
                            <field name="doctors_id"/>
                            <field name="patient_id" widget="many2one_avatar" options="{'preview_image': 'patient_image'}"/>
                        </group>
                        <group string="Appointment Information">
                            <field name="date"/>
                            <field name="description" widget="radio"/>
                        </group>
                        <notebook>
                            <page string="Notes">
                                <group>
                                    <field name="notes"/>
                                </group>
                            </page>
                        </notebook>
                        <field name="active" invisible="1"/>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="message_ids"/>
                    <field name="activity_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record model="ir.ui.view" id="view_hospital_appointment_tree">
        <field name="name">hospital.appointment.tree</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <tree decoration-primary="state == 'draft'"
                  decoration-success="state == 'confirmed'"
                  decoration-info="state == 'done'"
                  decoration-danger="state == 'cancel'"
                  decoration-warning="state =='in_consultations'">
                <field name="sequence" widget="handle"/>
                <field name="ref"/>
                <field name="date"/>
                <field name="doctors_id"/>
                <field name="patient_id" widget="many2one_avatar"/>
                <field name="activity_ids" widget="list_activity"/>
                <field name="description"/>
                <field name="state" decoration-primary="state == 'draft'"
                       decoration-success="state == 'confirmed'"
                       decoration-warning="state =='in_consultations'"
                       decoration-info="state == 'done'"
                       decoration-danger="state == 'cancel'"
                       widget="badge"/>
                <groupby name="patient_id">
                    <button name="action_test" string="Click Me" type="object" class="oe_highlight"/>
                    <button name="edit" type="edit" icon="fa-edit" title="Edit"/>
                </groupby>
                <field name="progress" widget="progressbar"/>
            </tree>
        </field>
    </record>
    <record model="ir.ui.view" id="view_hospital_appointment_search">
        <field name="name">hospital.appointment.search</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <search>
                <field name="ref" filter_domain="['|', ('id', 'ilike', self), ('ref','ilike', self)]"/>
                <field name="doctors_id"/>
                <field name="patient_id"/>
                <separator/>
                <filter name="deserves_treatment" string="Deserves Treatment"
                        domain="[('state', '=', 'deserves_treatment')]"/>
                <filter name="recovered" string="Recovered" domain="[('state', '=', 'recovered')]"/>
            </search>
        </field>
    </record>
    <record id="hospital_appointment_calender_view" model="ir.ui.view">
        <field name="name">hospital.appointment.view</field>
        <field name="model">hospital.appointment</field>
        <field name="arch" type="xml">
            <calendar date_start="date" color="patient_id" mode="month">
                <field name="id"/>
                <field name="date"/>
                <field name="doctors_id"/>
                <field name="patient_id"/>
            </calendar>
        </field>
    </record>

    <record id="action_hospital_appointment" model="ir.actions.act_window">
        <field name="name">Appointment</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.appointment</field>
        <field name="view_mode">tree,form,calendar</field>
        <field name="search_view_id" ref="om_hospital.view_hospital_appointment_search"/>
    </record>

    <menuitem
            id="menu_appointment"
            name="appointment"
            parent="menu_hospital_appointment"
            action="action_hospital_appointment"
            sequence="10"
    />

</odoo>